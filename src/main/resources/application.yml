server:
  port: 8087
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://127.0.0.1:3306/hospital?useSSL=false&serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    jedis:
      pool:
        min-idle: 0
        max-idle: 8
        max-active: 8
data:
    mongodb:
      uri: mongodb://127.0.0.1:27017/logs
mybatis:
  type-aliases-package: com.newer.homework0812.domain
logging:
  level:
      com.newer.homework0812.mapper: debug #输出该包下debug级别及以上级别的日志记录
jwt:
  secret: newer
  expiration: 6400
auth:
  header: Authorization